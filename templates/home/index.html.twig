{% extends 'base.html.twig' %}

{% set title = 'Accueil' %}

{% block main %}
  <div class="d-flex">
    <div class="w-25 rounded-5 border border-3 border-secondary overflow-hidden home-link">
      <a href="{{ path('necesse_index') }}"><img src="{{ asset('build/images/necesse.png') }}" alt="Necesse" class="img-fluid" /></a>
    </div>
  </div>
{% endblock %}
